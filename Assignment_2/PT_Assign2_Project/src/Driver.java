/*
 * Class:		Driver
 * Description:	The class is responsible for running the methods for the program, the Driver
 * Author:		[Duncan Do] - [s3718718] 
 */

public class Driver 
{
	public static void main(String[] args) 
	{
		/*Creating a new MovieMasterSystem object 
		*to run the menu method*/
		MovieMasterSystem run = new MovieMasterSystem();
		run.menu();
		/*
		 * BEGIN
		 * 	CREATE a MovieMasterSystem object
		 * 	RUN the MovieMastersystem's menu method
		 * END
		 */
	}
}